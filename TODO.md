# 待處理事項

需要人為判斷或外部資料才能推進的項目。純技術性的修補不記在這裡，直接做掉。

---

## 1. 拿真實標案文件校準章節與用語

**狀態：** 待決策（需要你提供資料）
**影響：** 高——這是目前投報率最高的一項

### 現況

章節規格、SLA 項目、採購與資安要求都已逐條對照官方指引與範本查證過（工程會招標文件範本、資訊服務採購契約範本、政府資訊服務採購作業指引）。但**沒有任何一份機關實際發出的需求說明書、或實際驗收通過的交付文件**參與過校準。

這類文件通常只隨招標文件發給投標廠商，網路上幾乎不公開。接政府案的公司手上就有，外人拿不到。

### 差在哪裡

官方範本規範的是**契約與招標程序**，不規範交付文件長什麼樣。所以目前這些內容是「依通用實務自行撰寫」，沒有實例驗證：

- 各機關實際使用的**章節名稱**（例如叫「系統分析書」還是「系統分析設計書」，本專案只能列出契約範本的官方名稱與常見別名的對應）
- 承辦人**實際會退件的點**（現在的 `[退件]` 判準是從規範推導，不是從退件紀錄歸納）
- 交付文件的**實際顆粒度**（一份 SRS 是 40 頁還是 200 頁，直接影響骨架該留多少層）
- 驗收會議上**真的被問**的問題

### 要決策的是

保密邊界。招標文件通常附保密條款，交付文件更可能含機關內部資訊——而本專案的規則本身就寫明「不得寫入真實 IP、網路架構、帳號密碼原則、資安設定值」。

建議的折衷做法，由鬆到嚴：

| 做法 | 取得的資訊 | 風險 |
| --- | --- | --- |
| 只抄**章節標題階層**，不進任何內文 | 章節名稱、層級深度、順序 | 低 |
| 加上**表格的欄位名稱**（不含資料列） | 需求追溯表、驗收項目表的實際欄位 | 低 |
| 加上**用語習慣**（動詞、句型，人工去識別化） | 判定合格的寫法 | 中 |
| 提供完整文件 | 全部 | 高，不建議 |

前兩項幾乎不涉及機密，卻能解掉大部分落差。建議從那裡開始。

### 決定要做的話

挑 2～3 個不同機關、不同規模的案子（大小案的顆粒度差很多），把章節標題階層貼進來就行。校準完要同步的檔案：

- `skills/tw-gov-it-docs/references/02-交付文件規格.md`（各文件章節規格，主要落點）
- `skills/tw-gov-it-review/references/01-結構與格式.md`（章節完整性檢核）
- `README.md`「目前的限制」第一條（校準後改寫）

---

## 2. 行動應用（App）的交付文件

**狀態：** 待決策（要不要做、什麼時候做）
**影響：** 中高——**現有骨架對 App 專案會給出錯誤結果**，不只是缺功能

### 現況

App 的**法規面**已收錄：行動應用 App 基本資安檢測基準、行動版 APP 無障礙開發指引，都在 `skills/tw-gov-it-review/references/03-法規速查.md` 裡。

但**文件面**完全沒有 App 的差異。

### 為什麼這條比另外兩塊未覆蓋範圍急

**上線切換計畫骨架直接不適用。** 該骨架的核心是「回復計畫」——出事時回復到舊版。App 沒有這回事：使用者裝置上的版本無法被收回，只能再送一版新的過審。照現有骨架寫出來的 App 上線切換計畫，回復程序那一段是假的，而那正是機關最會問的一段。

這是**會出錯**，不是**沒涵蓋**。

### 缺的欄位

- 雙平台（iOS／Android）版本與**相容範圍**（最低支援 OS 版本，會隨保固期變動）
- **上架審查期程**要算進履約期限——審查被退件不是廠商可控，須對應契約第七條（四）的展期事由
- 公務機、**MDM 派送**與企業版簽章憑證的到期管理
- 推播（含 APNs／FCM 憑證輪替）與**離線行為**
- **第三方 SDK 清單**與其蒐集之個資項目（資安檢測基準會查）
- 應用程式權限清單與索取理由（無障礙與個資都會問）
- App 版本與後端 API 版本的**相容矩陣**（舊版 App 不會消失，後端得同時支援）

### 要決策的是

做的話有兩種顆粒度：

- **小做**：在 `04-履約與維運文件.md` 加一節「App 專案的差異」，並在上線切換計畫骨架加警語與替代段落。成本低，先解掉「會出錯」的部分。
- **大做**：新增 App 專用骨架（上架計畫、相容矩陣、SDK 與權限清冊），並在兩個 SKILL.md 加路由。

建議先小做。等真的接到 App 案再視實際需求擴充——沒有實例支撐的骨架，會重蹈上面第 1 項的問題。

---

## 已排除（不需再議）

- **投標須知、廠商資格、契約條款**：由機關採購或總務單位依工程會範本產生，本組 skill 只檢查它們與工作說明書有無矛盾。
- **AI 專案與 AI agent 的文件層**：只覆蓋到採購與驗收標準層。可自主呼叫工具的 AI agent 在「公部門人工智慧應用參考手冊」範圍之外，權限邊界與工具呼叫稽核軌跡**無官方依據可引用**——在有官方規範前不宜自行撰寫，寫了就是無據可考的東西。
